This lambda_1 sub-folder contain the code related to preprocessing section of the pipeline, as well as the evaluation part. Architecture evaluation between x86 and arm64 are conduct by corresponding code, while details of the code are mostly the same.

For data scaling evaluation. The preprocessing code for enlarging the raw data is in scale_raw_data.py.

All of the code are made by Han Zhang.